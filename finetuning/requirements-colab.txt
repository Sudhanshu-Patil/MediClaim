# Fine-tuning dependencies — install in Colab, NOT in the local venv.
# (Colab ships torch with CUDA already.)
transformers>=4.46,<5
datasets>=3.0
peft>=0.13
trl>=0.12,<0.15
bitsandbytes>=0.44
accelerate>=1.0
huggingface_hub>=0.26
sentencepiece

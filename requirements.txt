# MedClaim-Agentic-RAG — Phase 1 (ingestion pipeline) dependencies.
# Python 3.10–3.12 recommended.

# Parsing (IBM Docling + TableFormer table-structure model)
docling>=2.15,<3

# Schema / config
pydantic>=2.7
python-dotenv>=1.0

# Stores & queue
qdrant-client>=1.12
neo4j>=5.24
redis>=5.0
celery>=5.4

# Embeddings — ONNX CPU runtime, no torch needed for ingestion
fastembed>=0.4
numpy>=1.26

# Token counting (chunker falls back to a heuristic if unavailable/offline)
tiktoken>=0.7

# Agent (roadmap step 4) + evaluation (step 7)
# Pinned as a set: ragas 0.2.x needs langchain-core 0.3.x, and langgraph 0.6
# is the newest line on that core (0.3 lacks __interrupt__; 1.x needs core 1.x).
langgraph>=0.6,<0.7
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-ollama>=0.2,<0.4
ragas>=0.2.14,<0.3
rapidfuzz>=3.0  # ragas non-LLM string-distance metrics
httpx>=0.27
# HITL checkpoint persistence (file-based; CLI --resume works across processes)
langgraph-checkpoint-sqlite>=2.0,<3
# Optional upgrade: Redis persistence (needs Redis Stack modules)
# langgraph-checkpoint-redis>=0.0.6

# Observability (roadmap step 6) — v2 SDK to match the self-hosted v2 server
langfuse>=2.53,<3

# API + UI + MCP (roadmap step 8)
fastapi>=0.115
uvicorn[standard]>=0.30
slowapi>=0.1.9
sse-starlette>=2.0
streamlit>=1.38
pymupdf>=1.24          # source-page rendering with bbox highlights
mcp>=1.2               # custom MCP server
edge-tts>=6.1          # free TTS endpoint

# Dev-only: generates the sample test PDF in scripts/generate_sample_pdf.py
reportlab>=4.1
